# Product Reviews ETL Pipeline

## Overview
Simple ETL pipeline with unit and integration tests.

## How to run

python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python data_pipelines.py

## Run tests

python -m pytest -v

# trigger actions
